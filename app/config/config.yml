imports:
    - { resource: parameters.yml }
    - { resource: security.yml }
    - { resource: competicionactual.yml }

framework:
    #esi:             ~
    #translator:      { fallback: %locale% }
    secret:          %secret%
    router:
        resource: "%kernel.root_dir%/config/routing.yml"
        strict_parameters: %kernel.debug%
    form:            true
    csrf_protection: true
    validation:      { enable_annotations: true }
    templating:      { engines: ['twig'] } #assets_version: SomeVersionScheme
    default_locale:  %locale%
    trust_proxy_headers: false # Should Request object should trust proxy headers (X_FORWARDED_FOR/HTTP_CLIENT_IP)
    session:         ~

# Twig Configuration
twig:
    debug:            %kernel.debug%
    strict_variables: %kernel.debug%
    globals:
      contact_email:         %contact_email%
      nombre_competicion:    %enroporra_ca_nombre%
      id_competicion:        %enroporra_ca_id%
      link_bases:            %enroporra_ca_link_bases%
      link_partidos:         %enroporra_ca_link_partidos%
      fecha_comienzo_fase_1: %enroporra_ca_fecha_comienzo_fase1%
      fecha_fin_fase_1:      %enroporra_ca_fecha_fin_fase1%
      fecha_comienzo_fase_2: %enroporra_ca_fecha_comienzo_fase2%
      primera_eliminatoria:  %enroporra_ca_string_primera_eliminatoria%
      proxima_competicion:   %enroporra_ca_string_proxima_competicion%

# Assetic Configuration
assetic:
    debug:          %kernel.debug%
    use_controller: false
    bundles:        [ ]
    #java: /usr/bin/java
    filters:
        cssrewrite: ~
        #closure:
        #    jar: %kernel.root_dir%/Resources/java/compiler.jar
        #yui_css:
        #    jar: %kernel.root_dir%/Resources/java/yuicompressor-2.4.7.jar

# Doctrine Configuration
doctrine:
    dbal:
        driver:   %database_driver%
        host:     %database_host%
        port:     %database_port%
        dbname:   %database_name%
        user:     %database_user%
        password: %database_password%
        charset:  UTF8

    orm:
        auto_generate_proxy_classes: %kernel.debug%
        auto_mapping: true

# Swiftmailer Configuration
swiftmailer:
    transport: %mailer_transport%
    host:      %mailer_host%
    username:  %mailer_user%
    password:  %mailer_password%
    spool:     { type: memory }
